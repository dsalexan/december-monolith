/**
 * "WHEN EVERYTHING GOES RIGHT"
 * initial value -> computation -> final value
 *
 * "WELL, SUCH IS LIFE"
 * initial value -> computation -> computed object
 *
 * *reacts to computed reference* -> computed object::computation -> final value
 */

/**
 * Usually a computation would invole a TREE and a TREE INTERPRETER.
 * But not always. So I will not attach it to this class.
 */

export * as Object from "./object"
export * as Value from "./value"

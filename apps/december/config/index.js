const VERSION = `0.1.0`
const MODULE_ID = `december`
const MODULE_NAME = `December Module Framework`
const TITLE = `December Module Framework`
const DESCRIPTION = `Whut`

// eslint-disable-next-line no-undef
module.exports = {
  MODULE_NAME,
  MODULE_ID,
  VERSION,
  TITLE,
  DESCRIPTION,
}

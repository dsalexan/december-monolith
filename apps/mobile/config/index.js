const VERSION = `0.1.0`
const MODULE_ID = `mobile`
const MODULE_NAME = `Mobile Compatibility Layer`
const TITLE = `Mobile Compatibility Layer`
const DESCRIPTION = `Whut`

// eslint-disable-next-line no-undef
module.exports = {
  MODULE_NAME,
  MODULE_ID,
  VERSION,
  TITLE,
  DESCRIPTION,
}
